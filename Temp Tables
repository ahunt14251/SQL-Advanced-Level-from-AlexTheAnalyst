#Temporary Tables are tables that are only visible to the session that created them, storing intermediate results for complex queries or to manipulate data before inserting it into a permanent table.

The two methods for creating temp tables
#1. To build it like a real table and insert data into it

CREATE TEMPORARY TABLE temp_table
(first_name varchar(50),
last_name varchar(50),
favorite_movie varchar(100)
);

SELECT *
FROM temp_table;
#Adding data

INSERT INTO temp_table
VALUES ('Aisling','Hunt','Jurassic World');

SELECT *
FROM temp_table;

#2. Build it by inserting data into it

CREATE TEMPORARY TABLE salary_over_50k
SELECT *
FROM employee_salary
WHERE salary > 50000;

SELECT *
FROM temp_table_2;
